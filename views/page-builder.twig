{% extends "base.twig" %}
{# Adds custom page class names to the body element for each page that way you can limit the scope of my CSS for each page. #}
{% set page_class = 'page-builder' %}
{##}
{% block content %}
	{% for item in post.get_field('content') %}
		{# Add New Modules Here #}
		{% include 'page-builder/hero/hero.twig' %}
		{% include 'page-builder/hero-slider/hero-slider.twig' %}
		{% include 'page-builder/columns/columns.twig' %}
		{% include '/single-cptcta.twig' %}
	{% endfor %}
{% endblock %}
